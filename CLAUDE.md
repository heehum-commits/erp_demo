# CLAUDE.md — 인사·총무 실습 앱

이 파일은 Claude Code가 이 저장소에서 작업할 때 따르는 규칙이다.
(말투·설명 방식 등 공통 규칙은 상위 폴더 `../CLAUDE.md` 참고)

## 프로젝트 개요

**단일 HTML 파일 앱**(`index.html`) 하나가 산출물이다. 빌드 도구·서버·프레임워크 없이 브라우저로 파일을 열면 그대로 동작한다. 홈 + 5개 탭(인사기록 / 근태·휴가 / 급여 / 비품·자산 / 법인카드)으로 구성되며, 입력 내용은 브라우저에 저장된다.

**모든 데이터는 교육용 가상 자료다.** 실재하는 인물·기업·급여·거래와 무관하다. 이 표기(화면 배너·인쇄 머리말·파일명 안내)를 지우지 않는다.

## 파일 구조

| 파일 | 용도 |
|---|---|
| `index.html` | 산출물 전체 (HTML·CSS·가상 데이터·로직이 한 파일) |
| `PRD.md` | 기획서. **코드·다른 문서와 어긋나면 PRD.md를 따른다** |
| `DESIGN.md` | 디자인 가이드. 디자인 작업 시 항상 참조 |
| `verify.js` | 자동 검증 도구 (`npm run verify`) |
| `data/가상_인사총무.json` | 원본 가상 데이터 |
| `build_artifact.py` | `index.html` → `dist/artifact.html` (링크 배포용 변환) |
| `gemini_summary.py` | 앱 데이터를 읽어 AI 브리핑 문장 생성 (`요약보기.bat`로 실행) |
| `gemini_call.py` | Gemini API 호출 예제 |
| `데이터안내.md` / `확인예시.md` / `레퍼런스.md` / `개발순서.md` | 교육용 설명 문서 |

## index.html 수정 시 필수 절차 — 항상 검증 후 보고

`index.html`을 조금이라도 수정하면, 완료 보고 전에 반드시 실행하고 **실제 출력을 근거로** 보고한다. "문제없어 보인다"처럼 실행 없이 추측으로 보고하지 않는다.

```
npm run verify
```

- 데이터 건수(직원 28 / 휴가 50 / 자산 35 / 소모품 12 / 카드내역 60 / 이력 20 / 급여 73), 홈의 "처리할 것" 카드 숫자, 실행 오류를 기준값과 비교한다.
- `node_modules`(jsdom)는 설치되어 있다. 없으면 `npm install` 한 번만 실행한다.
- `다름`이 나오면 원인을 고치거나, 의도된 변경이라면 **어떤 항목이 왜 달라졌는지** 사용자에게 명시적으로 설명한다.

## 데이터·계산 규칙

- **분모 규칙**: 비율의 분모는 항상 "해당 값이 확인된 건수"이며 화면에 분모를 표기한다. 전체 건수를 무조건 분모로 쓰지 않는다.
- **미기재 처리**: 값이 없으면 "미기재"로 남기고 해당 지표의 집계(분자·분모 모두)에서 제외한다.
- **계산은 앱이 한다**: AI(Gemini)는 이미 계산된 숫자를 문장으로 다듬는 역할만 한다. AI에게 숫자를 세게 하지 않는다 (PRD §5-8).
- **급여는 보기 전용**: 확정된 급여 금액은 화면에서 고치지 않고 자동 재계산도 하지 않는다 (PRD §2-2, §5-7).
- 건수(사람 수·오더 수)와 금액·일수는 서로 다른 지표이므로 섞어 쓰지 않는다.

## API 키 취급

- 키는 `.env`에만 둔다. **`index.html`을 포함한 어떤 산출물에도 키를 넣지 않는다.**
- `.env`는 `.gitignore`에 있어 git에 올라가지 않는다. 커밋 전 키가 섞이지 않았는지 확인한다.
- 앱에서 AI 기능을 쓰는 사람은 자기 키를 한 번 입력하고, 그 키는 그 브라우저에만 저장된다.

## 하지 말아야 할 것

- `dist/artifact.html`을 직접 수정하지 않는다. 자동 생성물이므로 `index.html`을 고친 뒤 `python build_artifact.py`를 다시 돌린다.
- 새 지표 추가, 시각 디자인 변경은 PRD 범위 밖이므로 사용자 확인 없이 진행하지 않는다.
- 상위 폴더 `../미사용/` 안의 자료는 참고하지 않는다.
