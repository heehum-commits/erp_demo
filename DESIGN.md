# DESIGN.md — 인사·총무 교육용 가상 데모 디자인 기준

> ⚠️ 이 앱은 **교육용 가상 자료**입니다. 화면 최상단 경고 띠는 어떤 경우에도 지우지 않습니다.

**적용 대상:** `인사총무_교육용가상데모.html`
**이 문서가 정하는 것:** 색 · 글꼴 · 여백 · 모서리 · 버튼 · 표 · 배지 등 **보이는 것만**
**이 문서가 정하지 않는 것:** 계산 기준 · 버킷 판정 · 데이터 규칙 → 전부 **`PRD.md` 5절이 우선**입니다.

---

## 0. 출처와 범위

- 참고 방향: **https://getdesign.md/cal/design-md**
  이 페이지가 밝히는 디자인 성격은 *"Clean neutral UI, developer-oriented simplicity"* — **무채색 중심의 담백한 도구 UI**입니다.
- ⚠️ 해당 페이지는 **실제 토큰 값(색상 hex, 타입 스케일, 여백 수치)을 공개하지 않습니다.**
  (`npx getdesign@latest add cal` 또는 유료 스타터킷으로만 배포)
  따라서 **이 문서의 모든 수치는 우리 앱에 이미 들어가 있는 실제 값**이며, 외부 수치를 지어내지 않았습니다.
  위 출처는 **"무채색·담백함"이라는 방향**에 대한 참고로만 남깁니다.

---

## 1. 색 (Color Tokens)

색은 전부 CSS 변수로만 씁니다. **hex를 코드에 직접 쓰지 않습니다.**

### 1-1. 밝은 모드 (기본)

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--canvas` | `#ffffff` | 페이지 바닥, 헤더 |
| `--soft` | `#fafafa` | 표 머리(th), KPI 상자, 줄 hover |
| `--card` | `#ffffff` | 카드·모달·입력칸 바탕 |
| `--strong` | `#f0f0f3` | 배지 바탕, 막대 트랙, 아바타 |
| `--ink` | `#171717` | 제목·강조 글자, **주 버튼 바탕**, 막대 채움 |
| `--body` | `#60646c` | 본문 글자 |
| `--muted` | `#999999` | 설명·보조 글자 |
| `--line` | `#ececf0` | 기본 1px 구분선 |
| `--line-strong` | `#dcdee0` | 버튼·입력칸 테두리 |

### 1-2. 의미색 (Semantic) — 뜻이 고정돼 있습니다

| 토큰 | 밝은 모드 | 어두운 모드 | **고정된 뜻** |
|---|---|---|---|
| `--ok` | `#16a34a` | `#4ade80` | 정상 · 승인 · 충분 · 보관중 |
| `--warn` | `#ab6400` | `#e0a33a` | 주의 · 대기 · 수리중 · 휴직 |
| `--err` | `#c4342f` | `#f08c88` | 문제 · 반려 · 지연 · 부족 · 폐기 |
| `--info` | `#0d74ce` | `#6cb6ff` | 진행중 · 불출중 · 본문 링크 |
| `--purple` | `#8145b5` | `#c49bea` | 연차에서 차감되지 않는 휴가 유형 |

> **뜻을 바꿔 쓰지 마세요.** 초록이 어떤 화면에서는 "승인", 다른 화면에서는 "부족"이 되면 안 됩니다.

### 1-3. 어두운 모드

`prefers-color-scheme` + `data-theme` 두 가지로 동작합니다.

| 토큰 | 값 | | 토큰 | 값 |
|---|---|---|---|---|
| `--canvas` | `#121214` | | `--ink` | `#f2f2f3` |
| `--soft` | `#17171a` | | `--body` | `#b0b4ba` |
| `--card` | `#1a1a1d` | | `--muted` | `#83868d` |
| `--strong` | `#26262c` | | `--line` | `#2a2a30` |
| | | | `--line-strong` | `#3a3a42` |

**규칙:** 모든 색은 **반드시 `:root`(밝은 모드)에 먼저 정의**하고, 어두운 모드 블록에서는 **같은 이름을 다시 정의**만 합니다.
어두운 모드 안에서만 존재하는 색을 만들면 밝은 모드에서 색이 사라집니다.

### 1-4. 교육용 경고 띠 전용색 — 다른 곳에 쓰지 않음

| 토큰 | 밝은 | 어두운 |
|---|---|---|
| `--edu-bg` | `#fff4d6` | `#3a2e10` |
| `--edu-fg` | `#7a4b00` | `#f0d191` |
| `--edu-line` | `#e8cf94` | `#5a4820` |

이 노란 띠는 **"이건 가짜 자료다"라는 표시 하나만** 담당합니다. 다른 안내에 쓰면 경고가 묻힙니다.

---

## 2. 글꼴 (Typography)

```
Inter, Pretendard, "Malgun Gothic", -apple-system, "Segoe UI", system-ui, sans-serif
```

**인터넷 없이 열리는 파일**이므로 웹폰트를 불러오지 않습니다. 설치된 글꼴을 순서대로 씁니다.

| 쓰임 | 크기 | 굵기 | 자간 |
|---|---|---|---|
| 처리할 것 숫자 | 30px | 600 | −0.03em |
| KPI 숫자 | 23px | 600 | −0.02em |
| 섹션 제목 (h2) | 19px | 600 | −0.02em |
| 모달 제목 (h3) | 17px | 600 | −0.02em |
| 카드 안 소제목 | 15px | 600 | −0.02em |
| 브랜드명 | 16px | 600 | — |
| **본문 (기본)** | **14px** | 400 | — |
| 표 내용 · 버튼 | 13px | 400 / 500 | — |
| 표 머리(th) · 설명글 | 12px | 600 / 400 | — |
| 배지 · 날짜 라벨 | 11px / 10px | 600 | — |

- 줄간격은 본문 **1.5** 고정.
- **큰 글자만 자간을 좁힙니다**(−0.02 ~ −0.03em). 본문은 건드리지 않습니다.
- 숫자가 들어가는 칸은 `font-variant-numeric: tabular-nums` — 자릿수가 흔들리지 않게 합니다.
- 굵기는 **400 / 500 / 600 세 가지만** 씁니다. 700 이상은 쓰지 않습니다.

---

## 3. 여백 (Spacing)

**4px 배수**만 씁니다.

| 값 | 쓰는 곳 |
|---|---|
| 4px | 배지 안쪽, 라벨과 입력칸 사이 |
| 8px | 필터 항목 사이, 버튼 사이 |
| 12px | 표 칸 좌우, 폼 칸 사이 |
| 14px | 카드 격자 간격 |
| 16 / 18 / 20px | 카드 안쪽 여백, 모달 안쪽 |
| 24px | 탭 내용 위쪽 여백 |
| 30px | **섹션과 섹션 사이** (기본 리듬) |
| 64px | 탭 내용 맨 아래 |

- 페이지 최대 폭 **1200px**, 좌우 여백 **20px**.
- 헤더 높이 **60px**, 상단 고정(sticky).

---

## 4. 모서리 (Radius)

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--r-sm` | **6px** | 작은 버튼(`.btn.sm`) |
| `--r` | **8px** | 버튼 · 입력칸 · 선택상자 · 아이콘 버튼 |
| `--r-lg` | **12px** | 카드 · 표 감싸개 · 모달 · KPI 상자 |
| — | **999px** | **배지와 막대에만** |

> **버튼을 알약 모양(999px)으로 만들지 마세요.** 둥근 알약은 배지·막대 전용입니다.

---

## 5. 버튼

| 종류 | 바탕 | 글자 | 테두리 | 크기 |
|---|---|---|---|---|
| `.btn` (기본) | `--card` | `--ink` | 1px `--line-strong` | 패딩 7×12px · 13px/500 · 반경 8px |
| `.btn.pri` (주 동작) | **`--ink`** | `--canvas` | `--ink` | 위와 같음 |
| `.btn.sm` (표 안) | `--card` | `--ink` | 1px `--line-strong` | 패딩 4×9px · 12px · 반경 6px |
| `.btn.ok` | `--card` | `--ok` | 1px `--line-strong` | 승인 · 반납 · 증빙 제출 |
| `.btn.danger` | `--card` | `--err` | 1px `--line-strong` | 반려 · 삭제 |
| `.icon` | 투명 | `--body` | 1px `--line-strong` | 34×34px · 반경 8px |

**규칙**
- 한 화면에서 **`.btn.pri`(검정 버튼)는 하나**만 둡니다. 그 화면에서 가장 하고 싶은 일 하나입니다.
- 색으로 뜻을 주는 버튼(`.ok` / `.danger`)은 **글자색만** 바꿉니다. 바탕을 칠하지 않습니다.
- 파란색(`--info`)은 **버튼에 쓰지 않습니다.** 링크와 정보 배지 전용입니다.
- hover는 `--strong` 바탕으로 살짝만. 주 버튼은 `opacity:.85`.

---

## 6. 입력칸 (Form)

| 항목 | 값 |
|---|---|
| 바탕 / 글자 | `--card` / `--ink` |
| 테두리 | 1px `--line-strong` |
| 패딩 · 최소 높이 | 7×10px · **34px** |
| 모서리 | 8px |
| 포커스 | `outline: 2px solid var(--ink)` (offset −1px) |
| 라벨 | 입력칸 **위에** · 12px/500 · `--body` · 간격 4px |

- 모달 폼은 **2열 격자**, 화면이 560px보다 좁으면 1열로 접힙니다.
- 부서·직급·유형처럼 **정해진 값은 반드시 선택상자(select)**로 받습니다. 직접 타이핑하게 두면 표기가 갈라집니다.

---

## 7. 카드 · 표 · 배지

### 카드
바탕 `--card` · 테두리 1px `--line` · 모서리 12px · 안쪽 여백 18px
그림자는 **한 단계만**: `0 4px 12px rgba(0,0,0,.06)` — 눌리는 카드(처리할 것)의 hover에만 씁니다.

### 표
- 감싸개에 `overflow-x:auto` + 모서리 12px → **좁은 화면에서는 표만 옆으로 스크롤**되고 페이지는 안 밀립니다.
- 표 최소 폭 680px · 칸 패딩 9×12px · 줄 구분선 1px `--line`
- 머리칸(th): 바탕 `--soft` · 12px/600 · `--body`
- 숫자 칸은 **오른쪽 정렬**(`.num`)
- 내용이 없으면 표를 비우지 말고 **`.empty` 안내문**을 한 줄 넣습니다.

### 배지 (`.bg`)
모서리 999px · 패딩 2×9px · 11px/600 · 바탕은 의미색의 **14% 투명도**, 글자는 의미색 원본.

### 아바타 (`.av`)
26px 원 · 바탕 `--strong` · 이름 **첫 글자 한 자**. 사진은 쓰지 않습니다(가상 인물이므로).

### 막대 (`.btrack` / `.bfill`)
트랙 `--strong` · 높이 16px · 모서리 999px
채움은 기본 `--ink`, 의미가 있을 때만 `--warn` / `--err` / `--ok`.
**막대 옆에는 항상 실제 숫자와 분모를 함께 적습니다** (예: `19% (3.5/19일)`).

---

## 8. 인쇄 (Print)

| 숨기는 것 | 남기는 것 |
|---|---|
| 헤더 · 탭 · 필터바 · 모든 버튼 (`.no-print`) | 모든 탭 내용(탭마다 새 페이지) |

- 교육용 경고 띠는 **인쇄물에도 반드시 남깁니다** (검은 테두리 2px로 강조).
- 인쇄 글자 11px · 표 머리 `#f2f2f2` · 테두리 `#bbb`.

---

## 9. 반응형

| 폭 | 달라지는 것 |
|---|---|
| 900px 이하 | 카드 4열·3열 → **2열** |
| 560px 이하 | 모든 격자 → **1열**, 모달 폼 1열 |
| 항상 | 표는 가로 스크롤, 페이지는 가로로 밀리지 않음 |

처리할 것 카드는 `repeat(auto-fit, minmax(188px, 1fr))` — 개수가 늘어도 알아서 줄바꿈됩니다.

---

## 10. 하지 말아야 할 것

- ❌ CSS에 hex 색을 직접 쓰기 → 항상 토큰(`var(--ink)`)
- ❌ 의미색의 뜻 바꾸기 (초록=승인, 빨강=문제 고정)
- ❌ 버튼을 알약 모양으로 만들기 → 8px 고정
- ❌ 파란색을 버튼 바탕으로 쓰기 → 링크 전용
- ❌ 한 화면에 검정 주 버튼 여러 개 두기
- ❌ 교육용 경고 띠 색을 일반 안내에 쓰기
- ❌ 웹폰트·외부 이미지·CDN 불러오기 → **파일 하나로 오프라인 실행**이 원칙
- ❌ 비율만 쓰고 분모 감추기 → 숫자와 분모를 같이 표기 (`PRD.md` 5절)
- ❌ 어두운 모드 블록에서만 색을 새로 만들기

---

## 11. 새 화면을 추가할 때 순서

1. 섹션 제목(`.sec-h`) + 한 줄 설명부터 쓴다 — **무엇을 보는 화면인지**
2. 필터가 필요하면 `.bar` 한 줄, 주 동작 버튼은 오른쪽 끝에 `.btn.pri` 하나
3. 표는 `.tw > table`, 숫자 칸은 `.num`
4. 표 아래 `.note`로 **분모·계산 기준**을 밝힌다
5. 교육용이라 오해할 수 있는 부분은 `.callout`으로 경고
6. 색이 필요하면 **새 색을 만들지 말고** 1-2절 의미색에서 고른다
